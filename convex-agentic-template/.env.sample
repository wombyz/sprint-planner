# ===========================================
# CONVEX CONFIGURATION (Required)
# ===========================================

# Convex deployment name - get this from `npx convex init`
# Format: <project-name>-<deployment-name>
CONVEX_DEPLOYMENT=

# ===========================================
# CONVEX AUTH (Required - set via Convex CLI)
# ===========================================
# These are SERVER-SIDE environment variables set via `npx convex env set`
# NOT in this .env file. See SETUP.md for instructions.
#
# AUTH_SECRET - Required for password authentication
#   Generate and set with: npx convex env set AUTH_SECRET "$(openssl rand -base64 32)"
#
# CONVEX_SITE_URL - Automatically set by Convex (no action needed)

# ===========================================
# ADW / CLAUDE CODE (Required for AI workflows)
# ===========================================

# Anthropic API key for Claude Code CLI
ANTHROPIC_API_KEY=

# Claude Code path - if 'claude' doesn't work, run 'which claude'
CLAUDE_CODE_PATH=claude

# Returns Claude Code to root directory after every command
CLAUDE_BASH_MAINTAIN_PROJECT_WORKING_DIR=true

# ===========================================
# GITHUB (Optional)
# ===========================================

# GitHub PAT - only needed if using different account than 'gh auth login'
GITHUB_PAT=

# Cloudflare tunnel token for webhook exposure
CLOUDFLARED_TUNNEL_TOKEN=

# ===========================================
# EXTERNAL APIS (Optional)
# ===========================================

# OpenAI - for Whisper transcription
OPENAI_API_KEY=

# Google Gemini - for image generation, etc.
GOOGLE_GEMINI_API_KEY=

# E2B - for cloud sandbox execution
E2B_API_KEY=
